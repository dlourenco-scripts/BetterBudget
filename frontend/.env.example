# Optional local override. If blank, Expo Go LAN sessions auto-detect your computer's Expo host.
# EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:4000/api/v1

# TestFlight / production builds should point at the hosted backend.
EXPO_PUBLIC_API_URL=https://api.betterbudget.app/api/v1
