# Local Expo Go / LAN backend.
# Copy this to .env.development and adjust the IP only when your computer's LAN IP changes.
# You can also leave EXPO_PUBLIC_API_URL unset and let Expo Go LAN auto-detect the host.
EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:4000/api/v1
